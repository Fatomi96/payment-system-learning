let promise,pg,monitor,config;_1e9‍.x([["default",()=>_1e9‍.o]]);_1e9‍.w("bluebird",[["default",["promise"],function(v){promise=v}]]);_1e9‍.w("pg-promise",[["default",["pg"],function(v){pg=v}]]);_1e9‍.w("pg-monitor",[["default",["monitor"],function(v){monitor=v}]]);_1e9‍.w("dotenv",[["config",["config"],function(v){config=v}]]);




const camelizeColumns = (data) => {
  const template = data[0] || {};
  Object.keys(template).forEach((prop) => {
    const camel = pg.utils.camelize(prop);
    if (!(camel in template)) {
      data.forEach((el) => {
        const d = el;
        d[camel] = d[prop];
        delete d[prop];
      });
    }
  });
};

config();

const options = {
  promiseLib: promise,
  receive: (data, result, e) => camelizeColumns(data),
};

monitor.setTheme('matrix');
monitor.attach(options);

const pgp = pg(options);
const db = pgp(process.env.DATABASE_URL);


_1e9‍.d(db);
