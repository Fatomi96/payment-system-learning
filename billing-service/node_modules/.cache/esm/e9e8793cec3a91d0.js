let ApiError,constants;_ec3‍.x([["default",()=>ErrorFactory]]);_ec3‍.w("../errors/api.error.js",[["default",["ApiError"],function(v){ApiError=v}]]);_ec3‍.w("../constants/api.message.js",[["default",["constants"],function(v){constants=v}]]);


const { RESOURCE_NOT_FOUND } = constants;

/**
 *Contains ErrorFactory methods
 * @class ErrorFactory
 */
               class ErrorFactory extends Error {
  static resolveError(error) {
    let message = 'Error while processing request. It is not you, it is us.';
    let status = 500;
    if (error.received === 0) {
      status = 404;
      message = RESOURCE_NOT_FOUND(error.resource);
    }
    return new ApiError({ message, status });
  }
}